packages:
  - 'apps/*'
  - 'packages/*'

allowBuilds:
  better-sqlite3: true
  cpu-features: true
  esbuild: true
  ssh2: true

# Native / postinstall build scripts we explicitly trust and allow to run.
onlyBuiltDependencies:
  - better-sqlite3
  - cpu-features
  - ssh2
  - esbuild
