# Freshness checkpoint — @pauldvlp/conventions references/freshness.md
last_reviewed: 2026-07-21
# open items (updated as they land):
# - [done] dropped EOL Node 20; added lint + format (ESLint flat + Prettier)
# - [done 2026-07-19] Vitest + unit suite (was ZERO tests): shared route/hostname logic, crypto service
# - [done 2026-07-21] integration: RoutesService.rebuildIngress vs a real throwaway SQLite with
#     Cloudflare doubled, and the putIngress catch-all contract with the SDK doubled. 23 tests.
# - next: Playwright on the SPA (backend with Cloudflare doubled)
# - next: the workflow_dispatch real-Cloudflare job — needs a DEDICATED test zone + a repo secret
#     (token scope: Zone:DNS:Edit + Account:Cloudflare Tunnel:Edit, scoped to that zone). Paul provisions.
# - TS pinned ^5.7 for NestJS decorator metadata; TS 7 is GA now — re-verify before touching
